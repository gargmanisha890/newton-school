/*Write code in js to find out most frequent item of an array. And also try to find out which numbers are prime 
and which are even, also find out vowels and consonants from the same array// input is [2,6,8,3,5,7,a,b,e,u,y,z
,i,o,8,8]//*/

var arr = [2, 6, 8, 3, 5, 7, 8, 8];
let counter = 0;
for (let i = 0; i < arr.length; i++) {
    if (arr[i] == arr) {
        counter++;
    }
    return

    //  if(arr[i]%2==0){
    //     console.log("this is even number" + arr[i]);

    // }
    // else{
    //     // counter++;
    //     //  console.log(counter);
    // }


}
